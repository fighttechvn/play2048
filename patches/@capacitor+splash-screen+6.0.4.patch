diff --git a/node_modules/@capacitor/splash-screen/CapacitorSplashScreen.podspec b/node_modules/@capacitor/splash-screen/CapacitorSplashScreen.podspec
index 0e5fab0..71f9349 100644
--- a/node_modules/@capacitor/splash-screen/CapacitorSplashScreen.podspec
+++ b/node_modules/@capacitor/splash-screen/CapacitorSplashScreen.podspec
@@ -11,7 +11,7 @@ Pod::Spec.new do |s|
   s.author = package['author']
   s.source = { :git => 'https://github.com/ionic-team/capacitor-plugins.git', :tag => package['name'] + '@' + package['version'] }
   s.source_files = 'ios/Sources/**/*.{swift,h,m,c,cc,mm,cpp}', 'splash-screen/ios/Sources/**/*.{swift,h,m,c,cc,mm,cpp}'
-  s.ios.deployment_target  = '13.0'
+  s.ios.deployment_target  = '12.0'
   s.dependency 'Capacitor'
   s.swift_version = '5.1'
 end
diff --git a/node_modules/@capacitor/splash-screen/ios/Sources/SplashScreenPlugin/SplashScreen.swift b/node_modules/@capacitor/splash-screen/ios/Sources/SplashScreenPlugin/SplashScreen.swift
index 4a00ed6..56792a1 100644
--- a/node_modules/@capacitor/splash-screen/ios/Sources/SplashScreenPlugin/SplashScreen.swift
+++ b/node_modules/@capacitor/splash-screen/ios/Sources/SplashScreenPlugin/SplashScreen.swift
@@ -120,10 +120,15 @@ import Capacitor
         var window: UIWindow? = UIApplication.shared.delegate?.window ?? nil
 
         if window == nil {
-            let scene: UIWindowScene? = UIApplication.shared.connectedScenes.first as? UIWindowScene
-            window = scene?.windows.filter({$0.isKeyWindow}).first
-            if window == nil {
-                window = scene?.windows.first
+            // UIWindowScene/connectedScenes are iOS 13+; use keyWindow on iOS 12.
+            if #available(iOS 13.0, *) {
+                let scene: UIWindowScene? = UIApplication.shared.connectedScenes.first as? UIWindowScene
+                window = scene?.windows.filter({$0.isKeyWindow}).first
+                if window == nil {
+                    window = scene?.windows.first
+                }
+            } else {
+                window = UIApplication.shared.keyWindow
             }
         }
 
diff --git a/node_modules/@capacitor/splash-screen/ios/Sources/SplashScreenPlugin/SplashScreenPlugin.swift b/node_modules/@capacitor/splash-screen/ios/Sources/SplashScreenPlugin/SplashScreenPlugin.swift
index 535ff4b..44c5287 100644
--- a/node_modules/@capacitor/splash-screen/ios/Sources/SplashScreenPlugin/SplashScreenPlugin.swift
+++ b/node_modules/@capacitor/splash-screen/ios/Sources/SplashScreenPlugin/SplashScreenPlugin.swift
@@ -68,11 +68,21 @@ public class SplashScreenPlugin: CAPPlugin, CAPBridgedPlugin {
             config.backgroundColor = UIColor.capacitor.color(fromHex: backgroundColor)
         }
         if let spinnerStyle = getConfig().getString("iosSpinnerStyle") {
-            switch spinnerStyle.lowercased() {
-            case "small":
-                config.spinnerStyle = .medium
-            default:
-                config.spinnerStyle = .large
+            // .medium/.large are iOS 13+; use the pre-13 equivalents on iOS 12.
+            if #available(iOS 13.0, *) {
+                switch spinnerStyle.lowercased() {
+                case "small":
+                    config.spinnerStyle = .medium
+                default:
+                    config.spinnerStyle = .large
+                }
+            } else {
+                switch spinnerStyle.lowercased() {
+                case "small":
+                    config.spinnerStyle = .gray
+                default:
+                    config.spinnerStyle = .whiteLarge
+                }
             }
         }
         if let spinnerColor = getConfig().getString("spinnerColor") {
